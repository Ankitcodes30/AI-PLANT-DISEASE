version: '3.8'

services:
  plantcare-ai:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./models:/app/models
      - ./app/static/uploads:/app/app/static/uploads
    environment:
      - FLASK_ENV=production
